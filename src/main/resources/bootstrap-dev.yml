spring:
  cloud:
    vault:
      config:
        lifecycle:
          enabled: false
      uri: http://localhost:8200
      authentication: approle
      kv:
        enabled: true
        backend: secret
        default-context: dev
        application-name: ""
      enabled: true
      app-role:
        role-id: ${ROLE_ID}
        secret-id: ${SECRET_ID}

